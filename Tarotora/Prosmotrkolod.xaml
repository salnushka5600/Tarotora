<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tarotora.Prosmotrkolod"
             Title="Prosmotrkolod" Background="LightPink">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">
            <Label FontSize="32" FontAttributes="Bold" HorizontalOptions="Center" TextColor="Black" />
            <Label Text="{Binding CategPicker}" TextColor="Black" FontSize="30" FontAttributes="Bold" HorizontalOptions="Center" />
            
            <CollectionView x:Name="CardsView" ItemsSource="{Binding Cards}" SelectionMode="None" HorizontalOptions="Center" ItemsLayout="VerticalGrid, 3">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="White" CornerRadius="20" Padding="15" Margin="10" HasShadow="True" WidthRequest="280" HorizontalOptions="Center">
                            <VerticalStackLayout Spacing="10">
                                <Image Source="{Binding Image}" Aspect="AspectFit" HeightRequest="200" />
                                <Label Text="{Binding Title}" FontSize="20" TextColor="Black" HorizontalOptions="Center" />
                                <Label Text="{Binding Description}" FontSize="14" TextColor="Gray" HorizontalTextAlignment="Center" />
                                <Button Text="Редактировать 🔧" BackgroundColor="HotPink" TextColor="White" CornerRadius="15" CommandParameter="{Binding}" Clicked="OnEditClicked" />
                                <Button Text="Удалить" BackgroundColor="HotPink" TextColor="White" CornerRadius="15" CommandParameter="{Binding}" Clicked="DeleteClicked" />
                                <Button Text="Сохранить" BackgroundColor="HotPink" TextColor="White" CornerRadius="30" CommandParameter="{Binding}" Clicked="SaveClicked" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
           
        </VerticalStackLayout>

    </ScrollView>
    
</ContentPage>


