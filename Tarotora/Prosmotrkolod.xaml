<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Tarotora.Prosmotrkolod"
    Title="Все карты" Background="LightPink"
    x:Name="ThisPage">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">
            <Label Text="Колода Таро" FontSize="32" FontAttributes="Bold" HorizontalOptions="Center" TextColor="Black"/>

            <CollectionView x:Name="CardsView" SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="3" HorizontalItemSpacing="10" VerticalItemSpacing="10"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="White" CornerRadius="20" Padding="10" HasShadow="True">
                            <VerticalStackLayout Spacing="10" VerticalOptions="FillAndExpand">
                                <Image Source="{Binding Image}" HeightRequest="100" WidthRequest="100" HorizontalOptions="Center"/>
                                <Label Text="{Binding Title}" FontSize="16" FontAttributes="Bold" TextColor="Black" HorizontalOptions="Center"/>
                                <Label Text="{Binding Description}" FontSize="12" TextColor="Gray"/>
                                <Label Text="{Binding Progress, StringFormat='Пройдено: {0}%'}" FontSize="12" TextColor="Blue"/>

                                <!-- Кнопки для админа -->
                                <HorizontalStackLayout Spacing="5" HorizontalOptions="Center">
                                    <Button Text="Редактировать 🔧" BackgroundColor="HotPink" TextColor="White" CornerRadius="10"
                                            CommandParameter="{Binding}"
                                            Clicked="OnEditClicked"
                                            IsVisible="{Binding Source={x:Reference ThisPage}, Path=IsAdmin}"/>
                                    <Button Text="Удалить ❌" BackgroundColor="LightCoral" TextColor="White" CornerRadius="10"
                                            CommandParameter="{Binding}"
                                            Clicked="OnDeleteClicked"
                                            IsVisible="{Binding Source={x:Reference ThisPage}, Path=IsAdmin}"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
