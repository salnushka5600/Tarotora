<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tarotora.Check"
             Title="Check" Background="LightPink">
    
    <VerticalStackLayout Padding="20" Spacing="20">

        <Label Text="📊 Прогресс пользователей"
               FontSize="28"
               FontAttributes="Bold"
               HorizontalOptions="Center"
               TextColor="Black"/>

        <Label Text="Выберите пользователя:"
               FontSize="18"
               TextColor="Black"
               HorizontalOptions="Center" />

        <CollectionView x:Name="UsersListView"
                        SelectionMode="Single"
                        SelectionChanged="OnUserSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BackgroundColor="White"
                           CornerRadius="20"
                           Margin="0,10"
                           HasShadow="True"
                           Padding="15">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="Selected">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="#FFD6F5"/>
                                        <Setter Property="BorderColor" Value="#FF69B4"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <HorizontalStackLayout Spacing="15">
                            <Image Source="user_icon.png"
                                   HeightRequest="40"
                                   WidthRequest="40"/>

                            <VerticalStackLayout>
                                <Label Text="{Binding Name}"
                                       FontAttributes="Bold"
                                       FontSize="20"
                                       TextColor="Black"/>

                                <Label Text="{Binding Subscribe, StringFormat='Подписка: {0}'}"
                                       FontSize="14"
                                       TextColor="Gray"/>
                            </VerticalStackLayout>
                        </HorizontalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Label x:Name="PercentageLabel"
               Text="Выберите пользователя 👆"
               FontSize="22"
               FontAttributes="Bold"
               TextColor="Black"
               HorizontalOptions="Center"
               Margin="0,10,0,0"/>

        <Slider x:Name="Slider"
                Minimum="0"
                Maximum="100"
                Value="0"
                IsEnabled="False"
                Margin="0,10,0,0"/>

        <Button Text="🔄 Обновить прогресс"
                Clicked="OnRefreshClicked"
                BackgroundColor="#FF69B4"
                TextColor="White"
                CornerRadius="20"
                FontSize="18"
                Padding="10,5"
                Margin="0,20,0,0"/>
    </VerticalStackLayout>
</ContentPage>